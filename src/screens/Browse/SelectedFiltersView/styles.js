import { StyleSheet } from 'react-native';

const s = StyleSheet.create({
  selectedFiltersView: {
    width: '100%',
    flexDirection: 'row',
    marginTop: 8,
  },
  selectedFiltersScrollView: {
    height: 36,
    flexDirection: 'row',
  },
  biggerSize: {
    height: 200,
  },
});

export default s;
